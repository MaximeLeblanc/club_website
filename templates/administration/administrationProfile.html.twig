{# templates/administration/administrationProfile.html.twig #}
{% extends 'common/base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/administration/administrator.css') }}">
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('js/administration/profile.js') }}"></script>
{% endblock %}
{% block navbar %}
    {{ include('administration/administrationNavbar.html.twig') }}
{% endblock %}
{% block body %}
<div class="row mt-3" id="editProfile">
    <div class="col-md-2"></div>
    <div class="col-12 col-md-8">
        <table class="table table-bordered">
            <tbody>
                <tr>
                    <td>
                        <h5 class="text-center">Profil</h5>
                        <form>
                            <div class="text-center mb-3">
                                <div id="photo">
                                    <div class="d-none" id="photoUrl">{{ admin.photo }}</div>
                                </div>
                                <label for="adminImageInput" class="btn btn-dark mb-1">Sélectionner une image</label>
                                <input type="file" class="d-none selectAdminImageInput" id="adminImageInput">
                                <br />
                                <div class="btn btn-dark disabled" id="deleteProfileImage">Supprimer l'image</div>
                            </div>
                            <div class="form-group">
                                <label for="editProfileName">Prénom</label>
                                <input type="input" class="form-control" id="editProfileName" placeholer"Prénom" value="{{ admin.name }}">
                            </div>
                            <div class="form-group">
                                <label for="editProfileLastName">Nom</label>
                                <input type="input" class="form-control" id="editProfileLastName" placeholder="Nom de famile" value="{{ admin.lastName }}">
                            </div>
                            <div class="form-group">
                                <label for="editProfileEmail">Email</label>
                                <input type="email" class="form-control" id="editProfileEmail" placeholder="Email" value="{{ admin.email }}">
                            </div>
                            <div class="form-group">
                                <label for="editProfilePassword">Mot de passe</label>
                                <input type="password" class="form-control" id="editProfilePassword" placeholder="Mot de passe">
                            </div>
                            <div class="row">
                                <div class="col">
                                    <div class="btn btn-dark float-left" id="editProfileButton">Modifier</div>
                                </div>
                                <div class="col">
                                    <div class="btn btn-dark float-right" id="cancelEditProfileButton">Annuler</div>
                                </div>
                            </div>
                        </form>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
    <div class="col-md-2"></div>
</div>
{% endblock %}